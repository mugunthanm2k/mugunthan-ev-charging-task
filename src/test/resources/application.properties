# @EmbeddedKafka automatically sets spring.embedded.kafka.brokers.
# This property reference ensures the app uses the embedded broker in tests.
spring.kafka.bootstrap-servers=${spring.embedded.kafka.brokers}
spring.jpa.show-sql=false
logging.level.com.tucker=WARN
logging.level.org.apache.kafka=WARN
logging.level.kafka=WARN
